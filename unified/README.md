# 🎒 まなびクエスト（統合版）

小学校の学習活動を **記録 → 可視化 → ゲーミフィケーション** する Google Apps Script 製 Web アプリです。
これまで別々に運用していた 3 つのプロジェクトを、**1 つのスプレッドシート・1 つの Web アプリ**に統合しました。

| 旧プロジェクト | 主な機能 | 統合後 |
|---|---|---|
| Manabi_Quest（学びクエスト） | 経験値・レベル・ガチャ・アバター・ミッション・バッジ | 「ホーム」「クエスト」「ひろば」タブ |
| assignment portfolio（学習の足あと） | タイピング・100マス計算・読書・成長・自主学習の記録 | 「きろく」タブ |
| performance portfolio（授業の記録） | 授業/テストのふり返り・道徳ノート・AI所見・PDF | 「きろく」タブ＋教員用画面＋シートメニュー |

## 統合で変わったこと（設計のポイント)

1. **データベースは 1 つ**
   すべてのシート（名簿・記録・ゲームデータ・所見）を 1 つのスプレッドシートに集約。`setupDatabase()` がワンクリックで全シートを自動生成します。
2. **バッチ処理・済フラグの廃止 → 保存時に即時経験値付与**
   旧版は「別シートの記録を定期バッチで走査して経験値化」していましたが、統合版は記録がアプリ内保存になったため、**保存した瞬間に経験値・ミッション進捗へ反映**されます。トリガー設定や外部シート ID の管理は不要です。
3. **記録は保護者向け資料・所見に活用**
   - 学期末に全児童分の **学習ポートフォリオPDF**（記録＋ふり返り＋ゲーム実績）を自動生成
   - ふり返り記述から **AI が所見材料を抽出** → 全体所見・道徳所見を AI 生成（Gemini / 任意機能）
4. **評価支援も自動化（旧「授業の記録」の成績処理機能を転記レスで再実装）**
   - 児童はテストのふり返りで**目標点**を立て、点数との差がバッジとグラフで見える（自己調整学習の支援）
   - 教員の「成績・評価」タブに、テストのふり返りから自動集計した**成績マトリクス（児童×単元・クラス平均・目標未達ハイライト）**を表示。旧版の「成績シートへの転記」作業そのものが不要に
   - 授業のふり返り保存のたびに「主体性の自己評価＋挙手回数（定量）」と「AIが採点する記述の深さ」を**学びに向かう力スコア**として自動蓄積（AI採点は所見材料抽出と同じ1回のAPI呼び出しに相乗り）。学期平均から**A/B/C評価案**をワンクリックで集計・シート出力
   - 所見ドラフトは**要録用（だ・である調）へのAI変換**もワンクリック
5. **AI所見材料の自動ストック（旧「授業の記録」の週案照合を強化して内蔵）**
   - 教員が「指導事項」シート（アプリの「AI所見」タブから登録可）に単元名・ねらいを登録しておくと、児童が授業/テストのふり返りを**保存した瞬間**に AI が記述を分析
   - 指導事項と照合したうえで、学習指導要領の**3観点タグ・おすすめ度つき**のエピソードとして「所見材料」シートへ自動ストック（めあての達成・挙手回数・目標点との差などの定量データも分析に使用）
   - 保存時に処理できなかった分は「所見抽出」フラグが空のまま残り、教員画面のボタンまたはシートメニューの一括抽出があとから回収
   - ストックした材料から、教員画面のワンクリックで**全体所見のドラフトを AI 生成**（「全体所見」シートにも保存）。**全児童分の一括生成**・**道徳所見の生成**・**要録用文体への変換**にも対応
   - 指導事項は表計算ソフトからの**貼り付け一括登録**が可能
6. **児童の学びを深める仕掛け**
   - **ふり返り質ボーナス**: AIが評価したふり返りの深さに応じてボーナス経験値。「たくさん書く」から「深く書く」への動機づけ（設定でON/OFF・係数調整可、追加API不要）
   - **書き出しヒント**: ふり返り入力欄にワンタップの書き出しチップ（「今日わかったのは…」等）。記述の質が上がり、AI抽出・評価の精度も向上
7. **教師の負担を減らす運用支援**
   - **声かけリスト**: 無記録が続く／授業「むずかしかった」が連続／テスト目標に連続未達の児童を、ダッシュボードに自動表示（ルールベース・AI不要）。カードから児童詳細へ即移動
   - **週次クラスサマリーメール**: 毎週月曜の朝、先週の記録件数・活発／無記録の児童・声かけリスト・所見づくりの状況を担任へ自動メール（時間主導トリガー）
   - **面談メモPDF**: 児童詳細からワンクリックで、成績（目標→結果）・がんばり・所見材料・最近のふり返り＋メモ欄をA4一枚に
   - **がんばり賞状PDF**: 学期末に児童ごとの賞状（ベスト記録・獲得バッジ・AIが作った成長エピソード）を一括生成
   - **年度末アーカイブ**: 記録・ログ・所見を別スプレッドシートへ退避し、記録シートを空にして新年度をまっさらに（マスタ・設定は保持）
8. **AI連携の堅牢化**
   - 既定モデルを `gemini-2.0-flash` に更新。レート制限（429）や一時エラーは**指数バックオフで自動再試行**し、一括処理の取りこぼしを軽減
9. **学習アプリ連携 — study.v1 共通学習ログの収集・活用**（詳細は後述の専用セクション）
   - GIGA山 学習アプリ群（Qalc・漢字タウン・けいさんカード・さんすうブロック・100マス計算）が端末に保存する**共通学習ログ（study.v1）の受信側（収集サーバー）**をこのアプリに内蔵。既存の8種類の手入力の記録はそのまま維持されます
   - 教員用「学習アプリ」タブで、**初回正答率・学習時間・クラスのつまずき問題**（何人が同じ問題を落としたか）を横断集計。児童詳細・週次サマリーメールにも反映
   - 受信したログは活動時間に応じて**経験値に変換**され、アプリ外の学習もゲーミフィケーションにつながります

## セットアップ手順

### 1. スプレッドシートとスクリプトの準備

1. 新しい Google スプレッドシートを作成する
2. 「拡張機能 → Apps Script」を開き、このフォルダの各ファイルを作成する
   - `.gs` ファイル → そのままの名前でスクリプトファイルとして追加（**01→09 の順に作成**）
   - `index.html` / `css.html` / `js_core.html` / `js_student.html` / `js_teacher.html` → HTML ファイルとして追加
   - `appsscript.json` → プロジェクト設定で「マニフェストを表示」を ON にして内容を置き換え
3. スプレッドシートに戻って再読み込みし、メニュー **「🎒 まなびクエスト管理」→「① 初期セットアップ」** を実行
   → 全シートがヘッダー・初期設定・サンプルミッション/バッジつきで生成されます

### 2. 名簿の登録

「児童マスタ」シートに入力します。

| 出席番号 | 名前 | ニックネーム | メールアドレス | 累計経験値 | 経験値 | 交換ポイント | 最終ログイン日 |
|---|---|---|---|---|---|---|---|
| 1 | 山田太郎 | たろう | taro@example.jp | 0 | 0 | 0 | |
| `担任` | 佐藤先生 | さとう | sato@example.jp | | | | |

- **出席番号に「担任」と入れたアカウントが教員用画面**になります
- 児童マスタにないアカウントはアプリを利用できません（登録案内が表示されます）

### 3. Webアプリとして公開

「デプロイ → 新しいデプロイ → ウェブアプリ」

- 実行ユーザー: **自分（デプロイするユーザー）**
- アクセスできるユーザー: **組織内の全員**（学校ドメイン）

発行された URL を児童に配付（Classroom 等に掲載）します。

### 4. 任意設定

| 機能 | 設定方法 |
|---|---|
| アバター画像 | ドライブのフォルダに `0001.png` 形式で画像を置き、「初期設定」にフォルダ ID を記入 → メニュー「② アイテム画像のIDを自動登録」。アイテムは「アイテムマスタ」に登録（カテゴリはアバター構成シートの列名と対応） |
| AI 所見機能 | Apps Script の「プロジェクトの設定 → スクリプト プロパティ」に `GEMINI_API_KEY` を追加 |
| AI所見材料の自動抽出 | 「初期設定」の `AI所見材料の自動抽出` が `ON`（既定）だと、ふり返り保存時に自動でAI分析（`OFF` にすると一括抽出のみ）。「AI所見」タブで指導事項（単元・ねらい）を登録すると照合精度が上がります |
| 道徳AIフィードバック | 「初期設定」の `道徳AIフィードバック` を `ON` に（保存時に自動生成。Classroom コース ID を設定すると個別配信も可能） |
| 学びに向かう力の評価基準 | 「初期設定」の `人間性評価_A基準` `人間性評価_B基準`（学期平均の閾値）、自己評価◎◯△の点数・挙手最大加点・AI評価最大点を調整可能 |
| ふり返り質ボーナス | 「初期設定」の `ふり返り質ボーナス`（ON/OFF）と `ふり返り質ボーナス係数`（深さ×係数=ボーナスEXP） |
| 声かけリストのしきい値 | 「初期設定」の `声かけアラート_無記録日数` `声かけアラート_連続未達回数` |
| 週次サマリーメール | シートメニュー「📧 週次サマリーメール → 自動送信を設定」で毎週月曜朝に担任へ配信（担任のメールを児童マスタに登録しておく） |
| 学習アプリ連携 | 「初期設定」の `学習ログ送信キー` に合言葉を設定＋受信用デプロイ（アクセス: 全員）を作成 → 後述「学習アプリ連携」参照 |
| 経験値のバランス | すべて「初期設定」シートで調整可能（係数・固定値・ガチャ排出率など） |

## 画面構成

### 児童用
- **低学年対応: 漢字に自動ふりがな** — 児童画面に出るすべての漢字の上に、ひらがなのルビを自動表示（教科名・単元名・本のタイトル・AIコメント・自分のふり返りなど動的な文章も対象）。教員画面には付きません
- **ホーム** — レベル・経験値バー・アバター・ミッション・お知らせ・最近のできごと・バッジ
- **きろく** — 8 種類の記録入力（タイピング/100マス/読書/成長/自主学習/授業/テスト/道徳）と履歴・グラフ・目標管理
  - テストのふり返りは**目標点→結果**を記録し、目標との差バッジ＆教科別の点数・目標推移グラフで自己調整学習を支援
  - **学習アプリ** — GIGA山学習アプリ群での学習記録（今週の回数・時間・はじめの1回で解けた率・最近の学習）を自分でふり返られる
- **クエスト** — ガチャ（1回/10連）・アイテム交換・アバター着せ替え・プロフィール
- **ひろば** — みんなのアバター・各種ランキング（経験値/今日のMVP/タイピング/100マス）

### 教員用
- **ダッシュボード** — クラス統計（今週の記録件数・活動児童数）・**声かけリスト（気になる児童の自動検出）**・児童一覧
- **児童の詳細** — 個人の全記録・ふり返り・活動ログを一覧（授業ふり返りは教科フィルター付き）＋**面談メモPDF**のワンクリック出力
- **AI所見スタジオ** — 指導事項の登録（1件ずつ／**貼り付け一括登録**）/ 未処理ふり返りの一括AI抽出 / 児童別の所見材料ストック閲覧・削除（3観点タグ・おすすめ度つき）/ 全体所見ドラフトのAI生成（個別・**全児童一括**）/ **道徳所見の生成** / **要録用（だ・である調）へのAI変換**
- **成績・評価** — テスト成績マトリクス（児童×単元・クラス平均・目標未達ハイライト）/ **学びに向かう力（主体的に学習に取り組む態度）のA/B/C評価案**の学期集計・シート出力
- **学習アプリ** — study.v1 共通学習ログの横断集計。児童別の回数・学習時間・**初回正答率**、アプリ別サマリ、**クラスのつまずき問題**（同じ問題を初回で落とした人数）、最近の学習一覧（期間: 今週/30日/すべて）
- **管理・出力** — ポイント配布 / お知らせ管理 / **学期末ポートフォリオPDF** / **がんばり賞状PDF** / 所見材料メモ

### スプレッドシートメニュー（教員）
- 初期セットアップ / アイテム画像 ID 登録
- AI 所見材料の抽出（未処理の授業・テストのふり返り → 所見材料シート）
- 全体所見・道徳所見の AI 生成（「生成」列にチェック → メニュー実行）
- 📧 週次サマリーメール（自動送信の設定／今すぐ送信／停止）
- 🗄️ 年度末アーカイブ（記録・ログ・所見を別スプレッドシートへ退避）

## データベース構成（全25シート）

| 分類 | シート |
|---|---|
| 共通 | 児童マスタ / 初期設定 / ログ |
| 学習の記録 | タイピング記録 / 100マス計算記録 / 読書記録 / 成長記録 / 自主学習記録 / 目標記録 |
| 学習アプリ連携 | 学習ログ（study.v1 共通学習ログの受信先） |
| ふり返り | 授業のふり返り / テストのふり返り / 道徳ノート / 道徳教材リスト / テスト単元リスト |
| ゲーム | アイテムマスタ / インベントリ / アバター構成 / ミッションマスタ / バッジマスタ / 獲得バッジ / お知らせ / プロフィール |
| 所見・評価 | 指導事項 / 所見材料 / 全体所見 / 道徳所見 / 学びに向かう力スコア |

※ このほか「人間性評価集計」シートは、教員画面から評価案を出力したときに自動生成されます。
※ 年度末アーカイブを実行すると、別ファイルとして「【アーカイブ】…」スプレッドシートが作られます。

## ミッション・バッジの条件キー

ミッションマスタ・バッジマスタの行を追加するだけで拡張できます。

**ミッション条件キー**: `RECORD_TYPING` `RECORD_CALC` `RECORD_READING` `RECORD_GROWTH` `RECORD_STUDY` `RECORD_LESSON` `RECORD_TEST` `RECORD_MORAL` `PLAY_GACHA` `TOTAL_EXP_WEEK` `TOTAL_STUDY_WEEK` `TOTAL_READING_WEEK` `TOTAL_CALC_WEEK`
（種別: デイリー / ウィークリー / 協力）

**バッジ条件キー**: `CURRENT_LEVEL` `TYPING_SPEED_MAX` `TYPING_COUNT` `CALC_COUNT` `CALC_PERFECT_COUNT` `READING_COUNT` `STUDY_COUNT` `GROWTH_COUNT` `LESSON_COUNT` `TEST_COUNT` `MORAL_COUNT` `LOGIN_STREAK_DAYS` `PROFILE_UPDATED` `PROFILE_COMPLETE` `GACHA_COUNT` `INVENTORY_COUNT` `MISSION_REWARD_COUNT`

## 学習アプリ連携（study.v1 共通学習ログの収集）

「学習ログ共通スキーマ仕様書 `study.v1`」に基づき、GIGA山 学習アプリ群
（gigayama.github.io 配下の Qalc / 漢字タウン / けいさんカード / さんすうブロック / 100マス計算）の
学習データを収集・活用します。まなびクエストは仕様でいう**受信側（サーバー / GAS）**にあたります。

```
学習アプリ（保存のみ / 匿名）
  └─ localStorage: study.records.v1
       └─ 送信ページ（../study-log-sender/ を gigayama.github.io に配置。出席番号を付与）
            └─ POST → まなびクエスト doPost（このアプリ）
                 ├─ 検証（仕様 §9: schema / UUID / appId / 数値範囲 / サイズ）
                 ├─ 重複排除（レコードID）・時刻の丸め（§4.1: 日付＋時間帯）
                 ├─ 「学習ログ」シートへ保存
                 └─ 活動時間 → 経験値付与（ゲーミフィケーション接続）
```

### セットアップ

1. スクリプト反映後、メニュー **「① 初期セットアップ」を再実行**（「学習ログ」シートと設定項目が追加されます）
2. 「初期設定」の **`学習ログ送信キー`** に合言葉を設定（空欄の間は受信を停止したままです）
3. **受信用のデプロイ**を追加作成: 「デプロイ → 新しいデプロイ → ウェブアプリ」で実行ユーザー **自分** / アクセス **全員**
   （児童用の本体デプロイ〔組織内〕とは別に共存できます。受信用 URL では児童マスタ未登録者はアプリを使えません）
4. 送信ページを `https://gigayama.github.io/study-log-sender/` に配置し、受信用 URL と送信キーを設定
   （手順の詳細は [`../study-log-sender/README.md`](../study-log-sender/README.md)）

### 集計の考え方（仕様に準拠）

- **主指標は「初回正答率」**（`firstTryCorrect / attempted`）。正解するまで先へ進めないアプリでも意味が揃う唯一の指標です（仕様 §2.7）
- 初回正答率は **通常出題（course）× 客観採点（objective）× ソロプレイ** のレコードだけで計算します。
  復習・にがて出題（母集団が偏る）、自己評価（採点の意味が違う）、対戦モード（妨害要素あり）は
  学力指標に混ぜず、**取り組み量（回数・時間）としてのみ**数えます（§2.4 / §2.9 / §3.2 / §5.5）
- **つまずき問題**は設問層（`items`）から「初回で誤答した児童の人数」を集計します。児童が自作した問題（custom）は
  設問IDの意味が児童ごとに異なるため対象外です（§2.4）
- 学習時間は `activeMs`（実際に操作していた時間）を優先し、なければ `elapsedMs` を使います（§2.8）

### プライバシー（仕様 §4）

- アプリ層・レコードには児童名を含めず、識別は送信ページが付与する**出席番号**のみ
- 学習開始時刻は既定で**「日付＋時間帯（午前/午後/夕方以降）」に丸めて保存**します（§4.1 の標準運用）。
  詳細な時刻が必要な場合のみ「初期設定」の `学習ログ時刻精度` を `分` に変更してください
- 受信時に既知のフィールドだけを残すため、想定外のデータは保存されません
- `source: "teacher"`（教師が入力した教材の操作記録）は受信時に破棄します

### 経験値への変換

受理したレコードごとに `floor(活動時間の分数 × 学習アプリ経験値係数)` の経験値を付与します
（1件あたり `学習アプリ経験値上限` まで、係数 0 で無効）。同一レコードIDの再送では二重付与されません。

## 旧版からのデータ移行

1. 統合版のセットアップ完了後、旧スプレッドシートの各記録シートの**データ行**を統合版の同名シートへコピー
   （列構成はほぼ同じです。統合版の「道徳ノート」は学年列を持たない点にだけ注意）
2. 旧・学びクエストの「児童マスタ」「インベントリ」「アバター構成」「獲得バッジ」「プロフィール」「ログ」も同様にコピー
3. 旧版の Google フォーム経由の提出を使っていた場合は、統合版ではアプリ内フォームに移行するため不要になります

## 統合版のアップデート（既存利用者向け）

スクリプトファイルを差し替えたあと、メニュー **「① 初期セットアップ」を再実行**してください。
「指導事項」「学びに向かう力スコア」「学習ログ」シート・不足しているヘッダー（ふり返りシートの「所見抽出」列、所見材料シートの拡張列など）・新しい設定項目（`AI所見材料の自動抽出`、`ふり返り質ボーナス`、`声かけアラート_*`、人間性評価の基準点、`学習ログ送信キー` などの学習アプリ連携設定）が、既存データを壊さずに追加されます。

なお、メール送信・トリガー設定のため `appsscript.json` に権限（`script.send_mail` / `script.scriptapp`）を追加しています。差し替え後に初めてアプリやメニューを実行する際、**再認証（権限の再承認）を求められます**。

## 技術メモ

- ランタイム: GAS V8 / タイムゾーン: Asia/Tokyo
- フロント: Bootstrap 5 + SweetAlert2 + Google Charts（SPA構成、`js_core` / `js_student` / `js_teacher` に分割）
- ふりがな: `kuroshiro` + `kuromoji`（CDN）で児童画面の漢字に `<ruby>` タグを自動付与。テキストノードだけを置換するためイベントやグラフを壊さず、入力欄・選択肢・グラフ描画先はルビ対象外。児童が入力した文章はHTMLエスケープしてから変換・注入するためXSSの心配なし。ライブラリが読み込めない環境（オフライン等）ではルビなしで通常動作。初回のみ辞書（数MB）を読み込み、以降はブラウザにキャッシュされます
- 書き込み系 API はすべて `withLock_()`（LockService）で排他制御
- 設定は `CacheService` で 5 分キャッシュ（変更後はメニューの「設定キャッシュをクリア」）
- メールアドレス比較はすべて小文字化・trim 済みで統一
